# Public values are compiled into the browser bundle during `npm run build`.
# RPC variables accept one or more comma-separated HTTP(S) endpoints; configure
# at least two providers per production chain for resilient reads.
NEXT_PUBLIC_SITE_URL=https://revnet.money
NEXT_PUBLIC_BENDYSTRAW_URL=https://bendystraw.up.railway.app
NEXT_PUBLIC_TESTNET_BENDYSTRAW_URL=https://testnet.bendystraw.xyz
NEXT_PUBLIC_PARA_API_KEY=
NEXT_PUBLIC_PARA_ENV=PROD
# Dedicated browser-visible key. Restrict its daily/monthly quota in Dwellir.
NEXT_PUBLIC_DWELLIR_API_KEY=
# Immutable release identifier, normally the Railway/Git commit SHA.
NEXT_PUBLIC_VERSION=development

# Runtime-only server credentials. Never pass these as Docker build arguments
# or prefix them with NEXT_PUBLIC_. Restrict the provider key to this app.
# Keep public pinning disabled unless the ingress enforces the rate/quota policy
# in DEPLOYMENT.md. Origin validation alone is not authorization.
IPFS_PINNING_ENABLED=false
IPFS_PINNING_EDGE_PROTECTED=false
FILEBASE_IPFS_RPC_TOKEN=
PINATA_JWT=
IPFS_PINNING_INGRESS_TOKEN=
