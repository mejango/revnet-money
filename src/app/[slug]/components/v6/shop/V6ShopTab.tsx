"use client";

import { ProjectItem } from "../shared";

// ponytail: temporary shell — replaced by the full website/-parity Shop tab
// (Inventory + Customers subtabs) in the v6 re-outfit waves.
export function V6ShopTab(_props: { projects: ProjectItem[] }) {
  return <div className="text-zinc-500">This project has no shop yet.</div>;
}
