"use client";

import { ProjectItem } from "../shared";

// ponytail: temporary shell — replaced by the full website/-parity Extras tab
// (payer-address deployment + indexed payer list) in the v6 re-outfit waves.
export function V6ExtrasTab(_props: { projects: ProjectItem[] }) {
  return <div className="text-zinc-500">Nothing here yet.</div>;
}
