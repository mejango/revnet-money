"use client";

import { ProjectItem } from "../../shared";

// ponytail: stub replaced by the settlement wave (across-chains, gossip, bridges,
// queued movements w/ local-proof claims).
export function V6SettlementSubtab(_props: { projects: ProjectItem[] }) {
  return <div className="text-zinc-500">Loading settlement…</div>;
}
