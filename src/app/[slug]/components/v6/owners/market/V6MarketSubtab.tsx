"use client";

import { ProjectItem } from "../../shared";

// ponytail: stub replaced by the market wave (AMM card + LP split-hook card).
export function V6MarketSubtab(_props: { projects: ProjectItem[] }) {
  return <div className="text-zinc-500">Loading market…</div>;
}
